# (Optionnel) Google Places API — uniquement si vous utilisez npm run fetch-google-reviews
# Sans clé : éditez src/data/google-reviews.json à la main (voir docs/avis-google.md)
# GOOGLE_PLACES_API_KEY=
# Optionnel si vous connaissez déjà le Place ID (sinon recherche textuelle automatique)
GOOGLE_PLACE_ID=
# Optionnel : requête de recherche si la fiche n’est pas trouvée automatiquement
# GOOGLE_PLACE_TEXT_QUERY=HFE Énergie 44 Rue Jean sans peur Lille
