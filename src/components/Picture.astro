---
/**
 * <Picture> sert automatiquement la version optimale (AVIF > WebP > original)
 * en utilisant l'élément <picture> natif.
 *
 * Les fichiers .webp et .avif sont générés par scripts/optimize-images.mjs
 * et placés à côté de l'original.
 *
 * Usage :
 *   <Picture src="/assets/pompeAChaleur/pompeAChaleur.jpg" alt="..." width={800} height={600} />
 */
export interface Props {
  src: string;
  alt: string;
  width?: number;
  height?: number;
  class?: string;
  loading?: "eager" | "lazy";
  decoding?: "sync" | "async" | "auto";
  fetchpriority?: "high" | "low" | "auto";
  sizes?: string;
}

const {
  src,
  alt,
  width,
  height,
  class: cls,
  loading = "lazy",
  decoding = "async",
  fetchpriority,
  sizes,
} = Astro.props;

// Convention : les variantes optimisées conservent l'extension d'origine
// dans leur nom (ex. equipe.jpg → equipe.jpg.webp / equipe.jpg.avif).
// Cela évite toute collision quand deux sources partagent le même nom
// mais une extension différente (equipe.jpg vs equipe.jpeg, camion.jpg
// vs camion.png, etc.). Voir scripts/optimize-images.mjs.
const webp = `${src}.webp`;
const avif = `${src}.avif`;
---

<picture>
  <source type="image/avif" srcset={avif} sizes={sizes} />
  <source type="image/webp" srcset={webp} sizes={sizes} />
  <img
    src={src}
    alt={alt}
    width={width}
    height={height}
    loading={loading}
    decoding={decoding}
    fetchpriority={fetchpriority}
    class={cls}
  />
</picture>
